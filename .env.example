# MENU SCAN frontend environment.

# Server-side API origin used for SSR/server components.
BACKEND_API_URL=http://localhost:4000

# Public API origin used by browser requests, including multipart image uploads.
# Set this to the deployed Render backend URL in production.
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:4000